# Feedback Center

User-scoped product feedback with administrative triage, search, filters, sorting, bulk
updates and immutable status history. Attachments are represented by storage-neutral metadata;
binary upload can be supplied later through an object-storage port without changing feedback
contracts. All mutations are also written to the platform Audit Log.
