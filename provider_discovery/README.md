# Provider Discovery

Synchronizes a configurable external provider catalog through a public source port. External
catalog code is never copied; only validated provider metadata enters the platform registry.
