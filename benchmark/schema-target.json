{
  "tables": {
    "benchmark_runs": {
      "columns": {
        "id": {"type": "INTEGER", "nullable": false},
        "engine_version": {"type": "VARCHAR(50)", "nullable": false},
        "calculated_at": {"type": "TIMESTAMP WITH TIME ZONE", "nullable": false}
      },
      "primary_key": ["id"]
    },
    "benchmark_entries": {
      "columns": {
        "id": {"type": "INTEGER", "nullable": false},
        "run_id": {"type": "INTEGER", "nullable": false, "foreign_key": "benchmark_runs.id"},
        "entity_id": {"type": "VARCHAR(300)", "nullable": false},
        "overall_rank": {"type": "INTEGER", "nullable": false}
      },
      "primary_key": ["id"],
      "unique_constraints": [["run_id", "entity_id"]]
    }
  }
}
