# Organization Workspace

Multi-user organization profiles, memberships, invitations, roles, limits, project links,
default-organization switching, and immutable activity history. The module owns only
organization collaboration state and keeps existing Workspace project APIs compatible.
