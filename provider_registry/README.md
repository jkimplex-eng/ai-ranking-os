# Provider Registry

Database-backed catalog of LLM providers exposed through public service and repository ports.
It stores provider-level capabilities, availability, free-tier status and routing priority.
Provider-specific SDKs and HTTP clients remain isolated in adapter modules.
